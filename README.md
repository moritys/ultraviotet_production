я совсем дурак и не сразу поняла, что не могу деплоить на локальный сервер,
так что данный воркфлоу будет работать только с глобальным ip

![workflow](https://github.com/moritys/ultraviotet_production/actions/workflows/main.yml/badge.svg)

# ultraviolet_production

## Реализованный проект

### Технологии

- Django 5.0.3

### Инструкции

1. Клонировать репозиторий

``` bash
git clone https://github.com/moritys/ProninTeamTest.git
```

2. Запустить контейнер

``` bash
(sudo) docker-compose up -d --build
```

3. Провести миграции внутри контейнера после его запуска

``` bash
(sudo) docker-compose exec web python manage.py migrate 
(sudo) docker-compose exec web python manage.py createsuperuser 
(sudo) docker-compose exec web python manage.py collectstatic --no-input
```

## Стадии работы с проектом

Базовое и приложения вносятся с админки

### Базовое

1. Добавить все виды плат
2. Добавить все виды этапов
3. Добавить компоненты и их количество
4. Добавить схемы производства: плата-этап-компонент-количество

### Регулярное

1. Внести новое приложение: номер, платы и их количество
2. Это количество плат фиксировано и должно продублироваться в этап "Новый заказ"
3. Далее проводить платы количеством через все этапы (учитывая производство)
4. Когда плата переходит в статус "Отгружено" и количество как в приложении,
то у приложения появляется отметка Готово и оно пропадает с главной страницы,
но остается в админке

Стадии изготовления:

Индикаторая плата

Состояния шлейфа: КАК ОСЛЕЖИВАТЬ В КАТАЛОГЕ
1. Шлейф неподрезанный
2. Шлейф подрезанный
3. Шлейф зачищенный
4. Шлейф залуженный
5. Шлейф запаянный (пропадает как отдельная сущность, не уверен нужно ли это состояние) ТУТ СПИСЫВАЮТСЯ шлейфы

Состояния платы (у неё две стороны)
6. Плата пустая
7. Плата запаянная (с одной стороны) ТУТ СПИСЫВАЮТСЯ комп (смд)
8. Плата запаянная (с обоих сторон) ТУТ СПИСЫВАЮТСЯ комп (смд)
9. Плата обесперемыченная
10. Плата прошитая
11. Плата со шлейфом (далее так же называем плата, но имеем ввиду, что шлейф припаян)
12. Плата с припаянными конденсаторами ТУТ СПИСЫВАЮТСЯ (выводной)
13. Плата протестированная (первично)
14. Плата лакированная 1 раз
15. Плата лакированная 2 раза
16. Плата протестированная (вторично)
17. Плата с запаянным дисплеем ТУТ СПИСЫВАЮТСЯ комп (дисплей)
18. Плата протестированная (третий раз)
19. Плата с подлаченным дисплеем
20. Плата с нанесенной на дисплей эпоксидкой
21. Плата с загерметизированным шлейфом
22. Плата готовая (не для отправки)
23. Плата готовая с наклейкой
24. Плата запаянная
25. Плата упакованная
